# ============================================================
# geo-insar Phase 1 依赖
# ============================================================
# 在 geo-insar 目录运行:
#   pip install -r requirements.txt
#
# Phase 2 还需要 pyroSAR + ESA SNAP(独立安装),见 plan §Phase 2 启动前置
# ============================================================

# ── HyP3 / ASF ────────────────────────────────────────
hyp3_sdk>=4.0.0
asf_search>=7.0

# ── 地理空间 ───────────────────────────────────────────
rasterio>=1.3
numpy>=1.24
shapely>=2.0
lxml>=4.9      # KML 解析

# ── Web + 任务持久化 ─────────────────────────────────
flask>=2.3
SQLAlchemy>=2.0
PyYAML>=6.0

# ── 网络 + 进度 ────────────────────────────────────────
requests>=2.31
tqdm>=4.65

# ── 元数据校验(可选,缺失时 fallback 到简易校验) ─────
jsonschema>=4.0
